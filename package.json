{
  "name": "preset-transfer",
  "version": "1.5.6",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "changelog": "node scripts/generate-changelog.js",
    "changelog:github": "node scripts/generate-changelog-from-github.js"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
